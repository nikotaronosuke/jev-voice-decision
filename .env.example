# Copy to .env (untracked) or export in your shell. Values are intentionally empty.
# Required: TypeSafe API key from the TypeSafe Console.
TYPESAFE_API_KEY=
# Optional: model alias (SDK default is jev-latest).
# TYPESAFE_DEFAULT_MODEL=
